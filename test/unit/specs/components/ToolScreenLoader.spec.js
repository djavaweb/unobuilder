import $ from 'jquery'
import Vue from 'vue'
import ScreenLoader from '@/components/tools/ScreenLoader'

describe('ScreenLoader => src/components/tool/ScreenLoader.vue', () => {
  it('should exists', () => {
    expect(ScreenLoader).to.be.exists
  })
})
