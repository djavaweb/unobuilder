import $ from 'jquery'
import Vue from 'vue'
import ScreenDetector from '@/components/tools/ScreenDetector'

describe('ScreenDetector => src/components/tool/ScreenDetector.vue', () => {
  it('should exists', () => {
    expect(ScreenDetector).to.be.exists
  })
})
